# Mika Bot 核心依赖
# 生成依据：扫描 src/plugins/ 下的 import 语句
# 版本同步自 pyproject.toml

# OneBot 协议支持
nonebot2>=2.4.4
nonebot-adapter-onebot>=2.4.6
nonebot-plugin-localstore

# 配置验证（使用 v2 API: field_validator, model_validator）
pydantic>=2.12.5

# 异步 HTTP 客户端（Gemini API / Serper 搜索）
httpx>=0.28.1

# 异步 SQLite（上下文 / 用户档案持久化）
aiosqlite>=0.22.1

# YAML 解析（提示词加载）
PyYAML>=6.0

# 图片处理（拼图功能）
Pillow>=10.0.0

# Gemini 模型（OpenAI 兼容格式）
google-genai>=1.58.0

# 环境变量加载
python-dotenv>=1.2.1

# 日志
loguru>=0.7.3

# FastAPI 驱动（NoneBot2 推荐）
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
