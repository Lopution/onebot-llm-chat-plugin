# Configuration (Generated)

This page will be generated from code schema to avoid documentation drift.

For now:
- Use `.env.example` as the source of truth.
- Use WebUI "effective config snapshot" for debugging.

Minimum required:

```env
LLM_API_KEY="YOUR_API_KEY"
MIKA_MASTER_ID=123456789
```

