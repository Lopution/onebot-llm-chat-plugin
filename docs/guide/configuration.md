# 配置参考（自动生成）

本页会在后续由代码 schema 自动生成，以避免“文档配置表过时”。

在此之前：

1. 以仓库根目录 `.env.example` 为准（只保留必填 + 常用项）。
2. 如需排障，请优先使用 WebUI 查看“生效配置（effective snapshot）”。

最小必填（只要这两项就能跑起来）：

```env
LLM_API_KEY="YOUR_API_KEY"
MIKA_MASTER_ID=123456789
```

